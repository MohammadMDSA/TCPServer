<Page
    x:Class="TCPServer.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TCPServer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
		<SplitView x:Name="SidePanel" HorizontalAlignment="Stretch" IsPaneOpen="True" VerticalAlignment="Stretch">
			<SplitView.Pane>
				<StackPanel HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Orientation="Vertical">
					<StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
						<ToggleButton Click="ToggleButton_Click" Content="&#xE700;" FontFamily="Segoe MDL2 Assets"/>
						<TextBlock x:Name="AppTitle" Text="[APP_TITLE]"/>
					</StackPanel>
					<ListBox x:Name="ScenarioListBox">
						<ListBox.ItemTemplate>
							<DataTemplate>
								<StackPanel Orientation="Horizontal">
									<TextBlock Text="{Binding Path=Title}"/>
								</StackPanel>
							</DataTemplate>
						</ListBox.ItemTemplate>
					</ListBox>
				</StackPanel>
			</SplitView.Pane>
		</SplitView>
	</Grid>
</Page>
